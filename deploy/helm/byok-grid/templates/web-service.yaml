apiVersion: v1
kind: Service
metadata:
  name: {{ include "byok-grid.fullname" . }}-web
  labels:
    {{- include "byok-grid.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - name: http
      port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "byok-grid.selectorLabels" (dict "root" . "component" "web") | nindent 4 }}
